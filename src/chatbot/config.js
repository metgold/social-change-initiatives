// Config starter code
import { createChatBotMessage } from "react-chatbot-kit";

const config = {
    botName: "Sochin",
    initialMessages: [createChatBotMessage(`Hi, my name is Sochin`)]
}

export default config